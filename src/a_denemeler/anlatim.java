package a_denemeler;

import java.util.Scanner;

public class anlatim {

    public static void main(String[] args) {



        // AND ==> "&&", If an operation contains
        // AND (&&) in order for the result to be true all of them should result to true


        //int a = 100;
        //int b = 50;

        //boolean c = ( a == b);
       // System.out.println(c);


       // Scanner date = new Scanner(System.in);

       // System.out.println("How many states are there in The USA");

      // int num = date.nextInt();

      // boolean result = (num == 50) && (true) && (num != 52);

        //System.out.println("your answer is " + result);



        //********************************

        // OR operation "||" the that OR works, if any of the comperassion is true it return true;




        //int d = 50;
       // int e = 30;
       // String t = "ugur";

        //boolean f = (d <50) || (e >= 30) && (t == "ugur") || ( t != "UGUR");
        //System.out.println(f);




        //*********************************



        // NOT "!"

       // String clr = "red";
        //String clr1 = "blue";

        //boolean result = !(4==5) && (clr != clr1);
        //System.out.println(result);


        //       Operator	         Name	                Description
          //       && 	            Logical and	       Returns true if both statements are true
         //        ||           	Logical or	       Returns true if one of the statements is true
        //         !	            Logical not	       Reverse the result, returns false if the result is true




    }






}
